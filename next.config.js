/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
          'res.cloudinary.com', 
          "picsum.photos"
        ]
      }
}

module.exports = nextConfig
